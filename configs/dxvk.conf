d3d9.maxEnabledLights = 64

# Tested 2026-08-29 against the black character texture, both verified PARSED in remix-dxvk.log,
# both with NO effect. Recorded so they are not retried:
#   d3d9.allowDiscard = False    (DISCARD would preserve contents)
#   d3d9.apitraceMode = True     (cached instead of write-combined memory for mapped resources)
# apitraceMode is reverted rather than left in place: it costs performance and bought nothing.
