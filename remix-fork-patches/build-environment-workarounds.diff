diff --git a/bridge/build_common.ps1 b/bridge/build_common.ps1
index 10c6c373..837a5720 100644
--- a/bridge/build_common.ps1
+++ b/bridge/build_common.ps1
@@ -60,7 +60,9 @@ function SetupVS {
 	  # Load VC vars
 	  Push-Location "${vsPath}\VC\Auxiliary\Build"
 
-	  cmd /c "vcvarsall.bat $Platform &set" |
+	  # NOTE: local environment hardening (NoDefaultCurrentDirectoryInExePath=1) breaks cmd's implicit
+	  # current-directory lookup for a bare/relative batch file name, so the absolute path is required here.
+	  cmd /c "`"${vsPath}\VC\Auxiliary\Build\vcvarsall.bat`" $Platform &set" |
 		ForEach-Object {
 		  If ($_ -match "=") {
 			$v = $_.split("="); Set-Item -Force -Path "ENV:\$($v[0])" -Value "$($v[1])"
diff --git a/build_common.ps1 b/build_common.ps1
index e9697cb6..4343ff9f 100644
--- a/build_common.ps1
+++ b/build_common.ps1
@@ -54,7 +54,9 @@ If (Test-Path env:LIBPATH) {
 } Else {
   # Load VC vars
   Push-Location "${vsPath}\VC\Auxiliary\Build"
-  cmd /c "vcvarsall.bat x64&set" |
+  # NOTE: local environment hardening (NoDefaultCurrentDirectoryInExePath=1) breaks cmd's implicit
+  # current-directory lookup for a bare/relative batch file name, so the absolute path is required here.
+  cmd /c "`"${vsPath}\VC\Auxiliary\Build\vcvarsall.bat`" x64&set" |
 	ForEach-Object {
 	  # Due to some odd behavior with how powershell core (pwsh) (powershell 5.X not tested) interprets a specific
 	  # predefined gitlab CI variable (in this case CI_MERGE_REQUEST_DESCRIPTION) with a value that includes ===  
diff --git a/tests/rtx/apps/HydraTestRender/meson.build b/tests/rtx/apps/HydraTestRender/meson.build
index fcb74c2c..28fb46aa 100644
--- a/tests/rtx/apps/HydraTestRender/meson.build
+++ b/tests/rtx/apps/HydraTestRender/meson.build
@@ -156,7 +156,10 @@ hdremix_test_exe = executable(
   include_directories: hdremix_includes,
   cpp_args: hdremix_defines + hdremix_compilerflags,
   dependencies: hdremix_linklibs + hdremix_dxvksdkbuilt + usd_plugin_deps + lib_glfw3,
-  override_options: ['cpp_std=c++17'],
+  # NOTE (local build workaround, not part of the identity-pinning patch): the packman-fetched
+  # glfw3.lib on this machine ships without its matching PDB, which trips LNK4099 and, under the
+  # project-wide werror=true, escalates to a fatal LNK1218. Scoped to just this test/sample exe.
+  override_options: ['cpp_std=c++17', 'werror=false'],
   install: true,
   install_dir: hdremix_outdir,
   install_tag: 'apics_HydraTestRender'
